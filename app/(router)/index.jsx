import { StyleSheet, Text, View } from 'react-native';
import React from 'react';
import LoginPage from '../presentation/screen/login';

const Login = () =>
{
   return (
      <LoginPage/>
   );
};

export default Login;

